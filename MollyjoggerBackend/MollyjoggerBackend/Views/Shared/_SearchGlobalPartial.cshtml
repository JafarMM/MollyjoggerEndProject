@{ 
    SearchViewModel searchViewModel = Model;
}
<ul>
    @foreach (var item in searchViewModel.ShopOfProducts)
    {
        <li>
            <a asp-controller="Shop" asp-action="Details" asp-route-id="@item.Id">@item.ProductName</a>
        </li>
    }
</ul>